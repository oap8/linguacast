# Row Level Security (RLS)

Principles:
- Users can only read/write their own progress
- Public content (podcasts, episodes) is readable
- Admin-only write access for content

All RLS policies must be explicit.
